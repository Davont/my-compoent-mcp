- 禁止在组件之间插入多余的 `<div>`、`<span>` 等包裹元素，严格按照组件文档示例中的嵌套结构使用。
- 必须使用上方提供的 import 语句，禁止自行更改导入路径或导入方式。
- 颜色、间距、圆角等样式值必须使用 Design Token（如 `var(--md-color-primary)`），禁止硬编码 `#1890ff`、`8px` 等具体值。
- 组件 Props 必须严格按照文档中的类型定义使用，禁止传入文档未列出的 Props。
- 禁止用 placeholder 替代 label，表单控件必须有明确的标签。
- 禁止自行封装已有组件的功能，如组件库已提供该能力则直接使用。
